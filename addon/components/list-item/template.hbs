{{#if (eq type 'checkbox')}}
  {{input
  checked=checked
  classBinding="fieldName"
  id=id
  name=name
  required=required
  type='checkbox'}}
{{/if}}

{{#if (eq type 'radio')}}
  {{radio-button
  classBinding="fieldName"
  groupValue=groupValue
  name=name
  radioId=id
  required=required
  value=fieldName}}
{{/if}}


<label for="{{id}}">{{labelText}}</label>